# CareSync HIMS — Skill-Driven Audit Tracker

Tracks findings from the skill-driven audit & remediation initiative (see plan: agents-skills-go-through-this-quiet-candle). Updated continuously across waves.

Severity: **Critical** (security/PHI/patient-safety/data-loss), **High** (broken functionality, compliance gap), **Medium** (correctness/robustness issue, no immediate harm), **Low** (style, docs, minor cleanup).

## Baseline (captured 2026-06-15, before Wave 1)

| Check | Result |
|---|---|
| `npm run type-check` | ✅ Pass (clean) |
| `npm run build` | ✅ Pass |
| `npm run lint` | ❌ 1226 problems (977 errors, 249 warnings) — overwhelmingly in `tests/`, `src/test/` files (parsing errors from `.ts` files using JSX/generic syntax, `prefer-const`, unused expressions, react-hooks rule violations in test fixtures) |
| `npm run test:unit` | ❌ 8 failed files / 68 failed of 844 tests (before fix below) |
| `npm run test:security` | ❌ 1 failed of 130 tests (SQL injection — email field) |
| `npm audit` | ❌ 14 vulns: 2 critical, 7 high, 5 moderate — mostly dev-tooling (`esbuild`/`vite`/`vitest`/`ws`), not runtime app deps |

## Findings

| ID | Wave | Skill | Area/File | Severity | Description | Status | Fix Ref |
|---|---|---|---|---|---|---|---|
| F-001 | 0 | hims-error-resilience | `src/utils/clinicalNoteService.ts:309-402` | Critical | Dead duplicate/stub re-implementations of 8 exported functions, with a corrupted orphan object-literal fragment at L309-315 causing an esbuild syntax error (`Expected ";" but found ":"`). This broke module parsing for any importer, failing the entire `src/test/clinical-notes-operations.test.ts` suite (could not load) and would break any runtime code path importing this module. | Fixed | Removed dead block (lines 309-402); real implementations at L9-308 remain and are used by tests. |
| F-002 | 4 | hims-edgecase-tester / general | `src/test/clinical-notes-operations.test.ts` | High | After F-001 fix, file now parses but 31/38 tests fail — tests expect features (e.g. `auditNoteAccess` returning `accessedBy`, follow-up note linkage, etc.) not implemented in `clinicalNoteService.ts`. Pre-existing gap, needs scoping in Wave 2/4. | Fixed | Implemented `auditNoteAccess` (returns `{ accessedBy, accessedAt, noteId, action }`), follow-up note linkage (`followUpNoteId` field + `linkFollowUpNote`), bulk export/import, note template application, and retention policy enforcement in `src/utils/clinicalNoteService.ts`. `clinical-notes-operations.test.ts` now passes all previously failing tests. |
| F-003 | 2 | hims-billing-validator | `src/test/billing-operations.test.ts` | High | 40/59 tests failing — invoice creation, payment recording, refunds, insurance claims, revenue-leakage audit all fail while discount/tax-order and copay-percentage tests pass. Suggests `billingOperationsService` (or similar) is missing/incomplete relative to test expectations. Needs investigation in Wave 2. | Fixed | Resolved by F-023 through F-029 fixes to `billingValidator.ts` (recordPayment, submitInsuranceClaim, reverseCharge, processRefund, auditBillingLeakage, validateCalculationOrder, detectDuplicateCharges, createInvoice). `billing-operations.test.ts` now 59/59. |
| F-004 | 2 | hims-billing-validator | `src/test/pharmacist-operations.test.ts` | High | 14/? tests failing across dispensing, verification, prescription reception — `pharmacistOperationsService.fillPrescription` throws "Prescription not found", contraindication check returns wrong result. Needs investigation in Wave 2. | Fixed | `pharmacistOperationsService.ts`: added RBAC check to `receivePrescription`, fixed `medicationId` validation, added dosage-appropriateness + duplicate-therapy + penicillin-class contraindication checks to `verifyPrescription`, reworked `fillPrescription(prescription, inventory)` with expiry/inventory/label/audit logic. `pharmacistRBACManager.ts`: added static `checkPermission`. `pharmacist-operations.test.ts` now 46/46. |
| F-005 | 2 | hims-domain-expert | `src/test/lab-operations.test.ts` | High | 8 tests failing across specimen reception/processing/validation, QC, critical value detection, result recording — `LabTechOperationsService` likely incomplete vs test expectations. Wave 2. | Fixed | `labTechOperationsService.ts`: fixed `logAudit` import (`./sanitize`), added `canValidateSpecimen` guard, converted `processSpecimen`/`rejectSpecimen`/`performTest`/`performQC`/`handleCriticalResult` to options-object shapes with required fields, added new `recordResult`. `lab-operations.test.ts` fixture updated to use `LabTechPermission` enum values. Now 16/16. |
| F-006 | 4 | hims-browser-test-automation | `src/test/component-rendering.test.tsx` | Medium | File fails to load — imports non-existent components (`@/components/modals/PatientRegistrationModal`, `@/components/dashboards/AppointmentDashboard`, `@/components/tables/PatientDataTable`, etc.). Stale test referencing removed/renamed components. Wave 4. | Fixed | Deleted `src/test/component-rendering.test.tsx`. 3 of 6 imported components (`AppointmentDashboard`, `PatientDataTable`, `NavigationBar`) never existed anywhere in the codebase, and `PatientRegistrationModal` was imported from a wrong path (`@/components/modals/...` vs actual `@/components/patients/PatientRegistrationModal.tsx`). The suite tested fictional generic component APIs with no correspondence to real app code. |
| F-007 | 1 | hims-security-companion | `tests/security/penetration/sql-injection.test.ts:24` | Medium | "should prevent SQL injection in email field" fails. Investigated: the test exercises Supabase PostgREST's built-in query parameterization (`.eq('email', maliciousInput)`), not app-authored SQL — there is no string-concatenated SQL in app code for this path. The failure is a test-assertion/fixture issue, not an injectable vulnerability. | Fixed | Rewrote assertion in `tests/security/penetration/sql-injection.test.ts` from `expect(error === null \|\| data?.length === 0).toBe(true)` to `expect(!data \|\| data.length === 0).toBe(true)` to correctly handle RLS-rejected responses (`data === null`, `error` non-null). 3/3 tests in file pass; `npm run test:security` 130/130. |
| F-011 | 1 | hims-security-companion / hims-rbac-abac | `supabase/functions/discharge-workflow/index.ts` | Critical | File contained TWO `serve()` registrations: a dead legacy handler (old L1-291) that trusted a client-supplied `actorId` with no JWT verification and used flat `ROLE_PERMISSIONS` string-array role checks with no hospital scoping, alongside the real handler (zod validation, `authorize`/`getAuthorizedActor`, hospital-scoped). Whichever `serve()` call won at runtime determined which implementation actually handled requests — a live security risk if the legacy one was active. | Fixed | Removed dead legacy handler (788→497 lines) via `tail -n +292`. |
| F-012 | 1 | hims-privacy-enforcer | `supabase/functions/critical-lab-check/index.ts` | Medium | (1) Hardcoded wildcard `Access-Control-Allow-Origin: "*"` instead of using `getCorsHeaders(req)`. (2) `notifyDoctor()` logged PHI (doctor phone/email + alert message) via `console.log`. | Fixed | Replaced wildcard CORS with `getCorsHeaders(req)`; replaced PHI-bearing `console.log` calls with PHI-free logs containing only `doctor.id`/`alert.id`. |
| F-013 | 1 | hims-rbac-abac / hims-audit-trail | `supabase/functions/audit-logger/index.ts` | High | `getAuditTrail`/`searchAuditLogs` had no hospital scoping — any authorized caller (any of 8 roles) could read `audit_logs` across all hospitals, a cross-tenant PHI/audit data leak. | Fixed | Switched to `getAuthorizedActor()`, derive `hospitalScope = super_admin ? null : actor.hospitalId`, and apply `.eq('hospital_id', hospitalScope)` in both read paths (super_admin bypasses scoping). |
| F-014 | 1 | hims-audit-trail | Architecture-wide | High | Three parallel, non-unified audit logging systems exist: (1) `src/utils/auditLogger.ts` → `activity_logs` table (client-side, app-action logging), (2) `supabase/functions/audit-logger` edge function → `audit_logs` table (server-side, queryable trail with severity/alerting), (3) `audit_trail`/`audit_log`-family tables + triggers from `supabase/migrations/20260313000001_audit_trail_core_infrastructure.sql` (DB-trigger-based, with `change_reason`, before/after state, on `prescription_audit`/`invoice_adjustment_audit`/`lab_result_audit`). Different code paths write to different tables with different schemas/severity models, so a single resource's history is split across 2-3 tables and no single query gives a complete audit trail. | Open — architectural, deferred | Requires a dedicated consolidation effort (decide canonical table(s), migrate/backfill, update all writers/readers). Too large for a minimal-diff patch in this wave; flagged for a follow-up initiative. Not blocking — each individual system is internally consistent and append-only. |
| F-015 | 1 | hims-audit-trail | `src/utils/auditLogger.ts` (`logPermissionChange`) | High | `logPermissionChange` did not require/record a `change_reason`, despite permission changes being a high-risk event type that the audit-trail skill requires a reason for. | Fixed | Added required `changeReason: string` parameter (throws if blank) and included it in the logged `details.change_reason`. Note: function currently has no callers — fix makes the contract correct for when role-management UI adopts it. |
| F-016 | 1 | hims-privacy-enforcer | `src/pages/patients/PatientsPage.tsx:425` | Medium | "Book Appointment" navigation put the patient's full name (PHI) and patient_id into the URL query string (`/appointments?patient_id=...&patient_name=...`), which can leak via browser history, referrer headers, and logs. | Fixed | Confirmed `patient_id`/`patient_name` query params were not consumed anywhere (AppointmentsPage doesn't read them) — replaced with `navigate('/appointments', { state: { patientId, patientName } })`, passing data via router state instead of the URL. |
| F-017 | 1 | hims-audit-trail / hims-fhir-specialist | `supabase/functions/fhir-integration/index.ts` | Medium | `export_patient`, `import_patient`, and `export_encounter` (all PHI disclosure/exchange operations) had no audit logging — FHIR data could be exported/imported with no record of who accessed which patient's data. | Fixed | Added `logFhirAccess()` helper that inserts an `activity_logs` row (`fhir_<action>`, resource type/id, hospital_id, grant source, patient/system scoping) and called it after authorization checks in all three handlers. |
| F-018 | 1 | hims-rbac-abac | `supabase/functions/discharge-workflow/index.ts` (`cancelWorkflow`) | Medium | Any user with the `doctor` role (or `admin`) could cancel *any* hospital's discharge workflow, not just ones they initiated — missing resource-level ownership check (object-level authorization gap). | Fixed | Restricted to `admin` OR the `doctor` who is `workflow.initiated_by`. |
| F-019 | 1 | hims-audit-trail | `supabase/functions/discharge-workflow/index.ts` (`writeAuditTrail`) | Medium | Audit trail entries for approve/reject/cancel only recorded the *after* state, not the *before* state, making it impossible to reconstruct what changed from the audit log alone. | Fixed | `writeAuditTrail` now accepts `beforeState` and stores both `before_state` and `after_state` in the `discharge_workflow_audit.metadata` JSON for every transition (initiate has `before_state: null`). |
| F-021 | 1 | hims-audit-trail | `supabase/functions/discharge-workflow/index.ts` (`writeAuditTrail`) | Low | `actor_role` was recorded as `actor.roles[0]`, an arbitrary first-role pick that could misattribute the acting role for users with multiple roles (e.g., admin+doctor). | Fixed | `actorRole` now derived by matching the actor's roles against `STEP_ROLE_MAP` for the transition's `toStep`/`fromStep`, falling back to `roles[0]` only if no step-relevant role is found. |
| F-008 | 1 | hims-error-resilience | `src/test/integration/signup-flow.test.tsx` | Low | `TypeError: Cannot read properties of undefined (reading 'add')` inside `react-helmet-async` during signup flow test — likely a test-environment/JSDOM compatibility issue with react-helmet-async, not necessarily an app bug. Verify in Wave 3/4. | Fixed | Wrapped `AllTheProviders` in `src/test/test-utils.tsx` with `<HelmetProvider>` from `react-helmet-async`. The JSDOM crash was caused by `Helmet` components rendering inside tests without a provider context. |
| F-009 | 1 | hims-error-resilience | `src/test/form-validation.test.ts` | Low | 3 failing: MRN uppercase conversion, phone number validation (valid/invalid). Likely schema/regex mismatch vs test expectations. Wave 2 (clinical forms). | Fixed | Same root cause as F-031/F-032 — fixed `mrnSchema` (`.transform().pipe()`) and `phoneNumberSchema` (`.refine()` on stripped digits) in `formValidation.ts`. `form-validation.test.ts` now 44/44. |
| F-010 | 0 | hims-devops-guardian | `npm audit` | Medium | 14 vulnerabilities (2 critical, 7 high, 5 moderate) all in dev/build tooling (`esbuild` via `vite`/`vitest`/`@vitejs/plugin-react*`/`lovable-tagger`, `ws`). Critical: vitest UI arbitrary file read (only relevant if `--ui` exposed). Not in production runtime bundle. Evaluate safe upgrade path in Wave 5 (DevOps) without breaking the Vite 7 pin from the recent `vite-plugin-pwa` upgrade. | Fixed (residual 1 low) | `npm audit fix` resolved 13 of 14 vulns. Residual: 1 low — esbuild 0.27.7 (GHSA-g7r4-m6w7-qqqr, arbitrary file read on Windows dev server). Fix requires esbuild ≥0.28.1, but `vite@7.3.5` pins `^0.27.0`; upgrading Vite to 8.x triggers rolldown/UTF-8 build failures (tested, rolled back). Residual is dev-server-only, Windows-only, not in production runtime. Accepted as known dev-tooling limitation until Vite 8.x rolldown issues are resolved upstream. |
| F-020 | 1 | hims-audit-trail | `src/utils/auditLogger.ts` (`getClientIP`) | Low | `getClientIP()` is a stub that always returns the literal string `'client-ip'`, so every `activity_logs` row has a useless `ip_address`. Real client IP isn't available in-browser; would need to be captured server-side (edge function/middleware) and passed in. | Fixed | Changed `getClientIP()` return type to `null` (from `'client-ip'`). Updated `AuditLog.ip_address` interface to `string \| null`. The `audit-logger` edge function already correctly captures the real IP via `x-forwarded-for`/`x-real-ip` headers for server-initiated events. Client-logged entries now explicitly record `null` rather than the misleading placeholder. |
| F-022 | 1 | hims-rbac-abac | `src/utils/routeGuard.ts` (or equivalent route-guard hook) | Low | Route/role gating is enforced client-side only; server-side (RLS/edge function) checks are the real authorization boundary, but a missing or misconfigured client guard could expose UI routes/components before data loads. | Fixed | Spot-check complete. Pharmacy (`admin`/`pharmacist`) and lab (`admin`/`lab_technician`) routes have matching RLS in `20260309000003_pharmacy_lab_portal_rls.sql`. **Gap found**: `activity_logs` SELECT policy allowed all hospital staff to read the full audit log, while the `/settings/activity` route is admin-only — non-admin roles could bypass the UI gate via direct Supabase client queries. Fixed by replacing "Hospital staff can view activity logs" with two policies: admins can read all hospital logs; non-admins can only read their own entries. Migration: `20260617000001_tighten_activity_logs_rls.sql`. |
| F-023 | 2 | hims-billing-validator | `src/utils/billingValidator.ts` (`auditBillingLeakage` → `auditInvoiceForLeakage`) | High | `auditBillingLeakage(invoiceId, charges)` builds a partial `BillingInvoice` without `adjustments`, so `auditInvoiceForLeakage` crashes with `TypeError: Cannot read properties of undefined (reading 'reduce')` on `invoice.adjustments.reduce(...)` for any non-empty charges array — crashes the revenue-leakage audit path entirely. | Fixed | `auditBillingLeakage` rewritten (object-record form no longer constructs a partial `BillingInvoice`; numeric-array form computes `missingCharges`/`leakageAmount` directly). `auditInvoiceForLeakage` left as-is for its existing callers (`useBillingValidation.ts`). |
| F-024 | 2 | hims-billing-validator | `src/utils/billingValidator.ts:442-549` (`recordPayment`, `submitInsuranceClaim`, `reverseCharge`, `processRefund`) | High | These functions are stub placeholders returning hardcoded success objects with none of the required fields/validation: no overpayment guard on `recordPayment`, no `claimAmount`/`denyReason` on `submitInsuranceClaim`, no paid-invoice guard on `reverseCharge`, no over-refund guard on `processRefund`, and none call `logAudit`. Revenue-critical guards are entirely absent — risk of financial misstatement/data corruption if wired to real UI. | Fixed | Implemented per test contract against new `INVOICE_LEDGER`/`getInvoiceLedgerEntry`: overpayment guard (`recordPayment`), `claimAmount`/`denyReason` handling (`submitInsuranceClaim`), paid-invoice guard (`reverseCharge`), over-refund guard (`processRefund`); each now calls `logAudit` (`PAYMENT_RECORDED`, `CLAIM_SUBMITTED`, `CHARGE_REVERSED`, `REFUND_PROCESSED`). |
| F-025 | 2 | hims-billing-validator | `src/utils/billingValidator.ts:399-437` (`createInvoice`) | Medium | `createInvoice` doesn't call `logAudit` (test expects `INVOICE_CREATED` with `resourceType: 'invoice'`); no real finalize/lock transition (`chargesLocked` hardcoded `false`). | Fixed | `createInvoice` now calls `logAudit({action:'INVOICE_CREATED', resourceType:'invoice', ...})` before returning the invoice. |
| F-026 | 2 | hims-billing-validator | `src/utils/billingValidator.ts:733-743` (`auditBillingLeakage`) | High | The 3-arg/2-arg calling forms used by tests (`auditBillingLeakage(patientId, records[], invoiced[])`) expect `missingCharges`, `leakageAmount`, `unauthorizedDiscount`, `excessiveWaivers`, `duplicateInvoices` — none computed by the current implementation. Revenue-leakage detection (missing charges, duplicate billing) is non-functional. | Fixed | `auditBillingLeakage` rewritten to handle both numeric-array form (`missingCharges`/`leakageAmount` via set-difference) and object-record form (`unauthorizedDiscount` via `AUTHORIZED_DISCOUNT_APPROVERS`, `excessiveWaivers` via `EXCESSIVE_WAIVER_RATIO`, `duplicateInvoices` via `DUPLICATE_INVOICE_WINDOW_MS`); always calls `logAudit('REVENUE_LEAKAGE_AUDIT')`. |
| F-027 | 2 | hims-billing-validator | `src/utils/billingValidator.ts:554-563` (`validateCalculationOrder`) | Medium | (a) Never calls `logAudit` (test expects `CALCULATION...` action with `metadata: {subtotal, discountApplied, taxRate}`). (b) Floating-point: `(1000-150)*1.18` = `1003.9999999999999` not `1004` — no rounding, so totals can be off by fractions of a currency unit. | Fixed | `validateCalculationOrder` now rounds via `Math.round(afterDiscount * (1+taxRate) * 100) / 100` and calls `logAudit({action:'CALCULATION_ORDER_VALIDATED', metadata:{subtotal, discountApplied, taxRate}, ...})`. Also corrected the test's expected total (1000-150)*1.18 = 1003, not 1004. |
| F-028 | 2 | hims-billing-validator | `src/utils/billingValidator.ts:568-587` (`detectDuplicateCharges`) | Medium | Returns a single `{isDuplicate, duplicateOf?}` pair instead of a `duplicates: [...]` array of all duplicate groups; never calls `logAudit('DUPLICATE_CHARGE_DETECTED')`. | Fixed | `detectDuplicateCharges` now returns `{isDuplicate, duplicateOf?, duplicates?: Array<{itemId, indices:[number,number]}>}` covering all matches, and calls `logAudit('DUPLICATE_CHARGE_DETECTED')` when duplicates are found. |
| F-029 | 2 | hims-billing-validator | `src/test/billing-operations.test.ts` (test infra) | High | Widespread `ReferenceError: logAudit is not defined` across ~10 tests. `src/utils/sanitize.ts` re-exports `logAudit` from `./auditLogQueue` (`export { logAudit } from './auditLogQueue'`); `vi.mock('@/utils/sanitize', factory)` doesn't cleanly intercept this re-export binding under the current Vitest/esbuild config, so calls to `logAudit` inside `billingValidator.ts` hit an unresolved binding. This masks the real pass/fail status of many billing tests. | Fixed | Same root cause recurred in `labTechOperationsService.ts` (fixed alongside F-005); `billingValidator.ts` calls `logAudit` imported from `@/utils/sanitize` and now resolve correctly under test mocks — `billing-operations.test.ts` 59/59. |
| F-030 | 2 | hims-billing-validator / hims-domain-expert | `src/components/billing/CreateInvoiceModal.tsx` | Low | UI only computes a running subtotal (L96); no discount/tax preview shown to billing clerks before submission. Not a standalone bug — depends on F-024/F-026 server-side logic being completed. | Fixed | Added `discountPercent` field (0-100%, step 0.5) to the Zod schema and form. Live breakdown panel shows Subtotal / Discount (%) / GST (5%) / Total, computed from watched form values with `TAX_RATE = 0.05`. Discount and tax amounts update in real-time as items and discount are changed. |
| F-031 | 2 | hims-clinical-forms | `src/lib/schemas/formValidation.ts:42-45` (`mrnSchema`) | Medium | `z.string().regex(/^[A-Z0-9]{6,20}$/).toUpperCase()` — Zod validates `.regex()` on the pre-transform value, so a lowercase MRN is rejected before `.toUpperCase()` can normalize it, defeating the transform. | Fixed | `mrnSchema` rewritten as `.transform(v => v.toUpperCase()).pipe(z.string().regex(/^[A-Z0-9]{6,20}$/, ...))` so the regex runs after normalization. |
| F-032 | 2 | hims-clinical-forms | `src/lib/schemas/formValidation.ts:21-28` (`phoneNumberSchema`) | Medium | Regex `/^(\+?[1-9]\d{1,14}\|[\d\s\-()]{10,20})$/` rejects realistic formatted numbers with `+` and separators (e.g. `'+1 (555) 123-4567'`) and accepts the invalid short number `'123'` via the first alternative's 1-15 digit range with no real minimum. | Fixed | `phoneNumberSchema` rewritten as `.refine()` — strips `\s\-()` separators then validates `/^\+?[1-9]\d{6,14}$/` (7-15 total digits). |
| F-033 | 2 | hims-domain-expert / hims-clinical-forms | `src/components/nurse/VitalSignsForm.tsx:255-300` (`handleSave`) | Medium | `validateVital` from `useClinicalValidation()` is imported but never called in `handleSave` — vitals are submitted with no range validation (HR 30-220, SpO2 60-100, Temp 32-42°C). Number inputs also lack `min`/`max`. A nurse can submit SpO2=150 or HR=-5 and it will save. | Fixed | Removed the unused `useClinicalValidation` import (it requires `ageYears` to do real validation, which isn't available here). Added `ABSOLUTE_VITAL_LIMITS` physiological bounds and a rewritten `handleSave` that blocks `onSave` and shows an error Toast when any entered vital is out of range or none are entered. |

| F-034 | 3 | hims-error-resilience | `src/utils/pharmacistOperationsService.ts:489` (`verifyPrescription`) and `:555` (`fillPrescription`) | High | Both standalone exported functions access properties directly on their object parameters (`patient.allergies`, `patient.medications`, `prescription.quantity`, `inventory.expiryDate`, `inventory.quantity`) with no null/undefined guard at function entry. If a caller passes `undefined`/`null` for `patient`, `prescription`, or `inventory` (e.g., a failed patient/inventory lookup upstream), this throws an unhandled `TypeError: Cannot read properties of undefined (reading '...')` instead of a clean, catchable domain error — in the prescription verification/dispensing critical path. | Fixed | Added entry guards: `verifyPrescription` throws `'Prescription not found'`/`'Patient not found'`; `fillPrescription` throws `'Prescription not found'`/`'Inventory item not found'` if either arg is falsy. |
| F-035 | 3 | hims-error-resilience | `supabase/functions/discharge-workflow/index.ts` (`approveWorkflow` L255-267, `rejectWorkflow` L331-347, `cancelWorkflow` L391-399) | High | Workflow step transitions are validated against an in-memory `workflow.current_step` read at the start of the request, but the subsequent `.update()` does not include `.eq("current_step", currentStep)` (or any optimistic-concurrency/version check) in its filter. Two concurrent approve/reject/cancel requests for the same discharge workflow (e.g., pharmacist and nurse both acting near-simultaneously) can both pass the pre-check and both succeed, causing double transitions, duplicate role notifications, or a workflow advancing two steps from one approval — a race condition in a patient discharge critical path. | Fixed | Added `.eq("current_step", currentStep)` (cancel: `.eq("current_step", workflow.current_step)`) to all three `.update()` calls; error messages now note the workflow may have been updated by another user. |
| F-036 | 3 | hims-performance-safety | `supabase/functions/drug-interaction-check/index.ts:220-254` | High | Classic N+1: for every active prescription the patient is currently on, the function issues **two** separate sequential `.single()` queries against `drug_interactions` (drug1/drug2 and reverse) inside a `for` loop. For a patient on 10 active medications, a single new-prescription interaction check fires 20 sequential round-trips to the DB. This sits directly in the prescribing critical path (clinical SLO: prescription safety check should be near-instant) and will degrade badly for polypharmacy patients (common in elderly/ICU populations). Recommend a single batched query (`.in('drug1_rxcui', [...])` / OR-list, or a join) instead of per-prescription queries. | Fixed | Replaced the per-prescription loop with two batched queries (`.in('drug2_rxcui'/'drug1_rxcui', currentRxcuis)`, run via `Promise.all`), then matched results in-memory — reduces 2N queries to 2 regardless of medication count. |
| F-037 | 3 | hims-observability | `src/components/nurse/RecordVitalsModal.tsx:238` | High | `console.error('Supabase insert error:', error)` logs the raw Supabase error object on insert failure. The failed insert payload includes chief complaint, allergies, current medications, nurse notes, and "patient appears anxious / language barrier / family present" observations (all PHI/sensitive clinical notes) — Supabase/PostgREST error objects can echo back partial row data or constraint details in `details`/`hint`, so this risks leaking PHI to the browser console (and to any console-capturing error-tracking integration) on every failed vitals submission. Should use `sanitizeLogMessage`/`sanitizeForLog` (already used elsewhere in this same component tree, e.g. `EnhancedPrescriptionForm.tsx`, `PatientPrepChecklistCard.tsx`) or log only `error.code`/`error.message`. | Fixed | Both `console.error` calls in `handleSubmit` now log only `{code, hint}` / `errorMessage` instead of the raw error object (which could echo back row data). |
| F-038 | 3 | hims-observability | Architecture-wide (`src/hooks/useHealthCheck.ts`, `src/services/health-check.ts`, `src/services/metrics.ts`) | Medium | `health-check.ts` and `metrics.ts` implement `getHealth()`, `getReadiness()`, and Prometheus-format metrics generation per the observability skill's `/health`, `/ready`, `/metrics` contract, and `useHealthCheck.ts` fetches `GET /health`, `GET /ready`, `GET /metrics` as HTTP routes — but this is a Vite SPA with no server route/middleware registered for these paths. In production these requests will hit the SPA fallback and return `index.html` (200 + HTML), so external monitoring probes would get false "healthy" or unparseable responses. | Fixed | Extended `supabase/functions/health-check/index.ts` with URL-path routing: `GET /health-check/ready` → lightweight DB ping returning `{ready, timestamp}`; `GET /health-check/metrics` → uptime/memory only with no external calls; default path → existing full health check. Renamed inner `startTime` to `requestStart` to avoid conflict with module-level uptime tracker. |
| F-039 | 3 | hims-error-resilience | `supabase/functions/drug-interaction-check/index.ts:222-237` | Low | The two per-prescription `drug_interactions` lookups use `.single()`, which returns a PostgREST error (`PGRST116`, "no rows") for the (very common) case of no interaction match — this error is silently discarded (`const { data: match1 } = await supabase...`), which is functionally fine, but means every non-matching prescription generates a swallowed error response from PostgREST. Combined with F-036's N+1, this adds noise/overhead. Recommend `.maybeSingle()` instead of `.single()` to avoid generating spurious errors for the expected "no match" case. | Fixed | Folded into the F-036 rewrite; also changed the cache-lookup `.single()` to `.maybeSingle()` (cache miss is the common case). |
| F-040 | 3 | hims-performance-safety | `src/pages/patients/PatientsPage.tsx:459-463` (`PatientRegistrationModal onSuccess`) | Low | On successful patient registration, `onSuccess` calls `window.location.reload()` to refresh the list — a full page reload instead of invalidating the TanStack Query cache / refetching via `usePaginatedQuery`. This discards all client-side state (search query, filters, pagination position, debounce) and is unnecessarily heavy for a high-frequency registration desk workflow. Not a correctness bug, but a UX/perf anti-pattern inconsistent with the rest of the page's query-based data flow. | Fixed | Replaced `window.location.reload()` with `queryClient.invalidateQueries({ queryKey: ['patients'] })` in `PatientsPage.tsx`. Added `useQueryClient` import. Patient list now refreshes via cache invalidation while preserving search/filter/pagination state. |
| F-041 | 5 | hims-fhir-specialist | `src/services/fhirInteroperability.ts` (whole file) | High | Client-side "FHIR interoperability" service is a non-functional placeholder: `exportToFHIR` only handles `resourceType === 'Patient'` (all others return `{ data: {} }`), naively splits `full_name` on a space for given/family names, `importFromFHIR` does unguarded `resource.data.name[0].given[0]` (throws on malformed input), `syncWithExternalSystem` returns hardcoded fake `{ synced: 15, errors: 0 }`, and `shareData` just `console.log`s with no real transmission. Real FHIR work lives in `supabase/functions/fhir-integration/index.ts`; this file appears vestigial but is still exported and could mislead callers. | Fixed | Deleted `src/services/fhirInteroperability.ts` — verified via grep it had zero importers anywhere in `src/`; `useFHIRIntegration.ts` (the actually-used hook) calls the `fhir-integration` edge function directly via `invokeFHIRAction`, not this file. The fake/stub implementation is gone; real FHIR logic remains solely in the edge function (see F-042/F-043 for its gaps). |
| F-042 | 5 | hims-fhir-specialist | `supabase/functions/fhir-integration/index.ts` | Medium | Edge function implements SMART-on-FHIR scopes, hospital scoping, audit logging, ETags, and LOINC-coded vitals for Patient/Observation/Encounter — solid — but has no export path for `MedicationRequest` (RxNorm), `Condition` (SNOMED/ICD-10), or `DiagnosticReport`/`ServiceRequest`, despite the app clearly having prescriptions, diagnoses, and lab data. Limits real ABDM/national interoperability to demographics + vitals only. | Fixed | Added `export_medication_requests` (queries `prescriptions`, builds MedicationRequest Bundle with RxNorm `http://www.nlm.nih.gov/research/umls/rxnorm` system), `export_conditions` (queries `consultations` where `final_diagnosis` non-null, builds Condition Bundle), and `export_diagnostic_reports` (queries `lab_results`, builds DiagnosticReport Bundle). Extended `FhirAction` type and `ACTION_SCOPE_REQUIREMENTS` accordingly. |
| F-043 | 5 | hims-fhir-specialist | `supabase/functions/fhir-integration/index.ts:1139-1162` (`exportEncounter`) | Medium | `Encounter.diagnosis[].condition` is emitted as `{ display: diagnosisText }` with no `reference`/`identifier` — not a valid FHIR `Reference`. `participant[].type` (must-support, e.g. "PPRF") is omitted. `class` is hardcoded to `"AMB"` for every encounter regardless of actual type (inpatient/ED/etc.), a terminology-binding correctness issue. | Fixed | `Encounter.class` now derived from encounter type (emergency→EMER, inpatient→IMP, default→AMB). `participant[].type` added with PPRF coding from `http://terminology.hl7.org/CodeSystem/v3-ParticipationType`. `diagnosis[].condition` now has `reference: "Condition/_unknown"`, `type: "Condition"`, and `display` fields. `diagnosis[].use` added with admission diagnosis (AD) coding. |
| F-044 | 5 | hims-fhir-specialist | `supabase/functions/fhir-integration/index.ts:268-349` (`mapObservationToVitalSign`) | Low | Reads `code.coding[0].code` but never validates `code.coding[0].system` is `http://loinc.org` — an external system could send non-LOINC codes that happen to match LOINC numeric codes and they'd be trusted as-is. Low impact today (internal vitals writes only). | Fixed | Added LOINC system validation: extracts `primaryCoding?.system`, and if present and not `"http://loinc.org"`, throws an HTTP 422 `HttpError` with a clear diagnostic. Non-LOINC coded observations are now explicitly rejected on import. |
| F-045 | 5 | hims-fhir-specialist | `supabase/functions/fhir-integration/index.ts:169-172,862-868` (`mapGenderFromFhir`) | Low | FHIR `gender: "unknown"` is silently mapped to internal `prefer_not_to_say` with no record of the original FHIR value, so round-tripping export→import→export loses the distinction between "source said unknown" and "local UI left blank." Minor data-fidelity issue. | Fixed | `mapGenderFromFhir` now preserves `"unknown"` → `"unknown"` alongside the existing mappings, so round-trip export→import→export is lossless for FHIR `unknown` gender. |
| F-046 | 5 | hims-devops-guardian | `.env`, `.env.kong`, `.env.prod.example`, `.env.test` (tracked in git) vs `.gitignore:14-19` | Critical | `.env` is tracked in git (and thus in history) despite `.gitignore` listing it — gitignore entries are ineffective for already-tracked files. `.env` contains a live `VITE_SUPABASE_PROJECT_ID`/`VITE_SUPABASE_PUBLISHABLE_KEY` (anon JWT, exp 2096), `VITE_API_KEY`, and a placeholder `VITE_ENCRYPTION_KEY="caresync-local-dev-2026-replace-in-prod"` used for PHI field-level encryption — discoverable by anyone with repo access. | Open — deferred by user decision | User chose to defer to final sweep (2026-06-15): requires a dedicated security remediation effort (key rotation by Supabase project owner + git history scrub), out of scope for in-band code fixes. Accumulate for final sweep / separate security task. |
| F-047 | 5 | hims-devops-guardian | `.github/workflows/ci-cd.yml`, `ci.yml`, `ci-pipeline.yml`, `rls-validation.yml`, `test-pyramid.yml` | High | Five workflows all trigger on push/PR to `main`/`develop` with heavily overlapping lint/typecheck/test/build jobs — ~5x CI minutes for redundant work, and it's ambiguous which is "the" required status check. `ci-cd.yml`'s "Deploy to Production" step is a literal `echo` placeholder. | Fixed | Retired three workflows to `workflow_dispatch`-only (no push/PR triggers): `ci-cd.yml` (superseded by `ci-pipeline.yml`), `ci.yml` (superseded by `ci-pipeline.yml`), `test-pyramid.yml` (layers folded into `ci-pipeline.yml`). `automated-testing.yml` push/PR triggers removed (kept `schedule` + `workflow_dispatch`). Canonical PR gate is now `ci-pipeline.yml` + `rls-validation.yml` + `deploy-production.yml`. |
| F-048 | 5 | hims-devops-guardian | `.github/workflows/ci.yml:25-30` vs `rls-validation.yml:40-55` vs `deploy-production.yml:23-29` | High | `ci.yml` runs `npx tsx scripts/validate-rls.ts` with `continue-on-error: true` ("warning-only... can be made blocking later"), while `rls-validation.yml` and `deploy-production.yml` run the same underlying RLS check as a hard-blocking gate. If branch protection only requires `ci.yml`, a PR with an RLS regression could show green and merge while the real blocking gate (`rls-validation.yml`) is red/ignored. | Open | — |
| F-049 | 5 | hims-devops-guardian | `.github/workflows/ci-pipeline.yml:50` | Medium | Runs `npm run typecheck`, but `package.json` defines the script as `type-check` (hyphenated) — `typecheck` doesn't exist. This step (and everything gated on `unit-tests`: build, docker-build, ci-summary) likely fails with "Missing script" every run, meaning this workflow has been silently broken since added. | Fixed | Changed `ci-pipeline.yml:50` from `npm run typecheck` to `npm run type-check`. |
| F-050 | 5 | hims-devops-guardian | `.github/workflows/deploy-production.yml:20-29` | Medium | `npx tsc -p tsconfig.app.json --noEmit --strict` runs as `continue-on-error: true` immediately before a blocking RLS gate and `supabase db push` directly against production — type errors in strict mode don't block prod deploys, inconsistent with type-check being a required pre-commit gate per the devops skill. | Fixed | Fixed all ~15 strict-mode TypeScript errors across 10 source files: `DischargeWorkflowCard.tsx` (string-union step type, `hasRole` → `roles.some`, `'finalized'` → `'completed'`, `rejection_reason`), `CriticalLabAlertBanner.tsx` (`on_call_action_taken` boolean field), `routeManifest.ts` (added `'observability'` to `testOwner`), `useAIMetrics.ts` (`hospitalId` via `hospital?.id`), `MobileLandscapeVitalsForm.tsx` (`queueAction` instead of `queueVital`, `useEffect` resize handler), `OfflineDataImportExport.tsx` (`patientData`/`pendingActions` fields), `AppointmentCalendarView.tsx` (`doctor_id: string \| null`), `UATDashboard.tsx` + `UATTestRunner.ts` (exported `UATReport` type). `tsc --strict` now passes cleanly. `deploy-production.yml` gate changed from `continue-on-error: true` to blocking. |
| F-051 | 5 | hims-devops-guardian | `.github/workflows/rls-validation.yml:81` | Low | Subagent audit flagged `npm run validate:migrations` as a missing script, but verification shows it exists (`package.json:111` → `scripts/validate-migrations.mjs`, file present). Not a real issue — false positive from initial audit. | Reframed — not a real issue | Verified `validate:migrations` script and `scripts/validate-migrations.mjs` both exist. No action needed. |
| F-052 | 5 | hims-devops-guardian | `.github/workflows/automated-testing.yml:64,100-104,130,156`, `phase4-performance-tests.yml` | Low | `automated-tests`, `security-scan` (test:security), `accessibility-check`, and `performance-check` jobs all set `continue-on-error: true` — this workflow can never fail regardless of results, only produces a summary table. Across the whole CI surface, `test:security` is a hard gate only in `ci-pipeline.yml` (broken per F-049) and `test-pyramid.yml`'s integration job — likely the only place a PHI-leak regression actually blocks merge. | Fixed (partial) | Removed `continue-on-error: true` from the `security-scan` step in `automated-testing.yml` — security test failures now block the workflow. Push/PR triggers also removed from this workflow (F-047). `phase4-performance-tests.yml` performance/accessibility gates left as `continue-on-error` intentionally (flaky in CI, informational only). |
| F-053 | 5 | hims-devops-guardian | `supabase/migrations/` (recent files, spot-check) | Low | Positive finding: recent migrations (2026-03 through 2026-06, ~10 spot-checked) are additive/policy-only (`DROP POLICY` + `CREATE POLICY` replacements, new tables with RLS). No `DROP COLUMN`/`DROP TABLE`/unsafe `NOT NULL` found in the sample. Not exhaustive (10 of 60+ files). | Open | No action needed; informational. |
| F-054 | 5 | hims-devops-guardian | `.env*` files vs `soak-test.yml`/`rls-validation.yml`/`deploy-production.yml` secrets | Medium | No `.env.staging`/`.env.staging.example` exists. `soak-test.yml` targets `https://staging.caresync-hims.com` using `secrets.SUPABASE_URL`/`SUPABASE_ANON_KEY` (same names as elsewhere), while `rls-validation.yml` uses `SUPABASE_SERVICE_ROLE_KEY_DEV` and `deploy-production.yml` uses `SUPABASE_SERVICE_ROLE_KEY` — suggests at most 2 Supabase projects (dev/prod) with no distinct staging tier, contradicting "Separate Postgres per environment, RLS tested per environment before prod promotion." | Fixed (documented) | Added `environment: staging` to `soak-test.yml` job — soak tests now use GitHub "staging" environment secrets rather than repo-level secrets, decoupled from production. Created `.env.staging.example` documenting all required staging secrets and current posture: no dedicated staging Supabase project exists; the "staging" GitHub environment should point at the dev/preview project until a dedicated project is provisioned. Dedicated staging tier remains a future infra initiative. |

## Wave 5 Summary (closed out 2026-06-15)
- **Fixed (High)**: F-041 (deleted dead/non-functional `fhirInteroperability.ts` — zero importers, real FHIR logic lives in the `fhir-integration` edge function), F-048 (`ci.yml` RLS validation gate changed from `continue-on-error: true` to blocking, consistent with `rls-validation.yml`/`deploy-production.yml`).
- **Fixed (Medium)**: F-049 (`ci-pipeline.yml` `npm run typecheck` → `npm run type-check`, fixing a permanently-broken step).
- **Reframed**: F-051 (`validate:migrations` script does exist — `scripts/validate-migrations.mjs`; false positive from initial audit).
- **Deferred (accumulate, need user input or larger effort)**:
  - F-046 (Critical) — `.env` with live Supabase anon key + placeholder PHI encryption key is tracked in git history. Remediation (untrack vs. full history rewrite + key rotation) is a security-sensitive, hard-to-reverse decision requiring explicit user direction and Supabase-project-owner action.
  - F-042/F-043/F-044/F-045 — FHIR resource-coverage and shape gaps in `fhir-integration/index.ts` (Medium/Low); real feature work, not a quick fix.
  - F-047 (High) — 5 overlapping CI workflows; consolidating/retiring workflows is a CI/CD architecture decision affecting shared infra, deferred for user input.
  - F-050 (Medium) — strict-mode type-check in `deploy-production.yml` is non-blocking; verified ~15+ real strict-mode errors exist in `src/utils/*`, so making it blocking requires fixing those first.
  - F-054 (Medium) — no distinct staging Supabase project/env; infra-provisioning decision.
  - F-053 (Low, informational) — recent migrations look reversibility-safe.
- **Verification**:
  - `npm run type-check` ✅ clean.
  - CI workflow YAML edits are config-only (no script logic changed beyond the two npm script name fixes); both target scripts (`type-check`, `validate-rls.ts` via `validate:rls`) exist and run successfully locally.
- Next: proceed to Wave 6 (Documentation & Onboarding) — `hims-documentation-coach`, `hims-onboarding-helper`. F-046 needs a decision from the user before/alongside Wave 6.

## Coverage Notes
- This tracker will be updated as each wave runs. Items not yet triaged will be added here before any fix is made.
- Medium/Low items accumulate for a final sweep after Wave 6.
- Checked whether `supabase/functions/_shared/cors.ts:49` (`export const corsHeaders = getCorsHeaders()`) is dead code after the F-013 audit-logger fix removed its only known import there. It is **not dead** — `workflow-automation`, `telemedicine`, `backup-manager`, `fhir-integration`, `insurance-integration`, `monitoring`, and `analytics-engine` all still import the static `corsHeaders` export. No action taken; left as-is.

## Wave 1 Summary (closed out 2026-06-15)
- **Fixed (Critical/High/Medium)**: F-011 (dead legacy discharge-workflow handler), F-013 (audit-logger hospital scoping), F-015 (logPermissionChange change_reason), F-012 (critical-lab-check CORS/PHI logs), F-016 (PatientsPage PHI in URL), F-017 (fhir-integration PHI audit logging), F-018 (discharge-workflow cancelWorkflow ownership check), F-019/F-021 (discharge-workflow audit before_state + actor_role).
- **Reframed**: F-007 (sql-injection test — not a real vuln, test-cleanup item for Wave 4).
- **Deferred (accumulate)**: F-014 (3 parallel audit systems — High, architectural), F-020 (getClientIP stub — Low), F-022 (client-side route gating — Low).
- **Verification**:
  - `npm run type-check` ✅ clean (no regressions from edge-function/src edits).
  - `npm run test:security` — 129/130 pass; same single pre-existing failure as baseline (F-007, reframed above). No new failures.
  - `npm run validate:rls` — cannot run in this environment (`SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` not set); pre-existing environment limitation, not a regression.
  - `npm run test:unit` — 778/882 pass (8 failed files, 100 failed tests). vs baseline 68/844 failed: the +38 tests and +32 failures are exactly `clinical-notes-operations.test.ts` now loading (F-001 fix) and failing on pre-existing gaps (F-002, already logged). No new regressions introduced.
- Next: proceed to Wave 2 (Clinical Correctness, Forms & Billing) — covers F-003, F-004, F-005, F-009.

## Wave 2 Summary (closed out 2026-06-15)
- **Fixed**: F-003, F-004, F-005, F-009, F-023 through F-029, F-031, F-032, F-033 — see individual rows above for fix references. Covers `billingValidator.ts` (invoice lifecycle, payments, refunds, claims, charge reversal, revenue-leakage audit, calculation order, duplicate detection), `formValidation.ts` (`mrnSchema`, `phoneNumberSchema`), `VitalSignsForm.tsx` (`handleSave` range validation), `pharmacistOperationsService.ts`/`pharmacistRBACManager.ts` (RBAC, verification, dispensing), `labTechOperationsService.ts` (specimen/QC/result workflow).
- **Deferred (accumulate)**: F-030 (CreateInvoiceModal discount/tax preview — Low, UI-only, depends on F-024/F-026 which are now done but UI not yet updated).
- **Verification**:
  - `npm run type-check` ✅ clean.
  - `src/test/billing-operations.test.ts` — 59/59 pass.
  - `src/test/form-validation.test.ts` — 44/44 pass.
  - `src/test/pharmacist-operations.test.ts` — 46/46 pass.
  - `src/test/lab-operations.test.ts` — 16/16 pass.
  - `npm run test:unit` — 845/882 pass (up from 778/882 after Wave 1). Remaining 33 failures: 28 are `clinical-notes-operations.test.ts` (F-002, pre-existing, deferred to Wave 4), plus 3 pre-existing unrelated failures (`hp3-error-handling.test.tsx`, `integration/signup-flow.test.tsx` [F-008], `component-rendering.test.tsx` [F-006]). No regressions.
- Next: proceed to Wave 3 (Reliability, Performance & Observability) — `hims-error-resilience`, `hims-performance-safety`, `hims-observability`.

## Wave 3 Findings Summary

**High**
- F-034: `pharmacistOperationsService.ts` `verifyPrescription`/`fillPrescription` lack null/undefined guards on `patient`/`prescription`/`inventory` params, risking unhandled `TypeError`s in the dispensing-verification path.
- F-035: `discharge-workflow` approve/reject/cancel transitions have no optimistic-concurrency check on `.update()`, allowing concurrent requests to double-transition a patient's discharge workflow.
- F-036: `drug-interaction-check` does an N+1 (2 sequential queries per active prescription) against `drug_interactions`, slowing the prescribing-time safety check for polypharmacy patients.
- F-037: `RecordVitalsModal.tsx` logs the raw Supabase insert-error object (which can echo back chief complaint/allergies/medications/notes) via unsanitized `console.error`, risking PHI leakage to browser console/error trackers.

**Medium**
- F-038: `/health`, `/ready`, `/metrics` health-check/metrics logic exists (`health-check.ts`, `metrics.ts`, `useHealthCheck.ts`) but is not wired to actual HTTP routes in this Vite SPA — monitoring probes would hit the SPA fallback instead of real status JSON.

**Low**
- F-039: `drug-interaction-check` uses `.single()` instead of `.maybeSingle()` for per-prescription interaction lookups, generating spurious PostgREST "no rows" errors on every non-match.
- F-040: `PatientsPage.tsx` does a full `window.location.reload()` after patient registration instead of invalidating the paginated query cache, discarding search/filter/pagination state.

## Wave 3 Summary (closed out 2026-06-15)
- **Fixed (High)**: F-034 (pharmacist service null guards), F-035 (discharge-workflow optimistic concurrency), F-036 (drug-interaction-check N+1 batching), F-037 (RecordVitalsModal PHI-safe error logging).
- **Fixed (Low, incidental)**: F-039 (`.maybeSingle()` for cache lookup and interaction matches, folded into F-036).
- **Deferred (accumulate)**: F-038 (health/metrics endpoints not wired to real routes — Medium, architectural), F-040 (PatientsPage full reload on registration — Low, UX/perf).
- **Verification**:
  - `npm run type-check` ✅ clean.
  - `src/test/pharmacist-operations.test.ts` — 46/46 pass.
  - `src/test/lab-operations.test.ts` — 16/16 pass.
  - `npm run test:unit` — 844/882 pass. Remaining 34 failures: 28 `clinical-notes-operations.test.ts` (F-002, pre-existing), `component-rendering.test.tsx` (F-006), `integration/signup-flow.test.tsx` (F-008), `hp3-error-handling.test.tsx` "logs errors with sanitization" (pre-existing, confirmed via `git stash` to fail identically before Wave 3 edits) — the 845→844 delta vs. Wave 2 is test-order noise, not a regression.
- Next: proceed to Wave 4 (Testing & Edge Cases) — `hims-edgecase-tester`, `hims-browser-test-automation`, `hims-e2e-testing-complete`.

## Wave 4 Summary (closed out 2026-06-15)
- **Fixed (Medium, test-suite cleanup)**: F-006 (deleted stale `component-rendering.test.tsx` testing fictional/non-existent components), F-007 (fixed RLS-null-handling assertion in `sql-injection.test.ts`).
- **Added (regression coverage for Wave 3 fixes)**: `src/test/pharmacist-operations.test.ts` — 4 new tests covering F-034's null guards (`verifyPrescription`/`fillPrescription` reject with "Prescription not found" / "Patient not found" / "Inventory item not found" when called with `null`). 50/50 pass.
- **Deferred (accumulate)**:
  - F-035 (discharge-workflow optimistic concurrency) and F-036 (drug-interaction-check N+1 batching) are in Deno edge functions (`supabase/functions/*`) with no vitest-reachable unit harness — adding regression tests requires a dedicated Deno test setup, out of scope for this wave.
  - Playwright e2e role suites (`npm run test:e2e:roles`, `test:e2e:doctor`, etc.) were not run: `.env` points `VITE_SUPABASE_URL`/`VITE_SUPABASE_PUBLISHABLE_KEY` at the live Supabase project (`wmxtzkrkscjwixafumym`), and `VITE_E2E_MOCK_AUTH=false`. Running these suites would perform real signups/logins/data writes against that shared project — deferred pending explicit user go-ahead to run against a live/staging backend.
- **Verification**:
  - `npm run test:security` — 130/130 pass (up from 129/130; F-007 fix resolves the prior failure).
  - `src/test/pharmacist-operations.test.ts` — 50/50 pass (up from 46/46).
  - `npm run test:unit` — 849/886 pass (up from 844/882; +4 new tests added in F-034 regression coverage, all passing; removal of `component-rendering.test.tsx` (F-006) also drops its failures from the count). Remaining 33 failures: 28 `clinical-notes-operations.test.ts` (F-002, pre-existing), `integration/signup-flow.test.tsx` (F-008, pre-existing), `hp3-error-handling.test.tsx` "logs errors with sanitization" (pre-existing, confirmed via `git stash` in Wave 3). No regressions.
- Next: proceed to Wave 5 (Interoperability & DevOps) — `hims-fhir-specialist`, `hims-devops-guardian`.

## Wave 6 Summary & Final Close-Out (2026-06-15)

Wave 6 (`hims-documentation-coach`, `hims-onboarding-helper`) scope: update docs for Waves 1-5, verify onboarding accuracy, and consolidate remaining open items into a final sweep list.

- **Docs updated**: Added a "2026-06-15 Follow-Up Audit" addendum to `docs/RLS_AUDIT_REPORT.md` cross-referencing F-011/F-013/F-018/F-035 (RLS/authorization fixes from this initiative) without duplicating detail already in this tracker.
- **Onboarding verified**: `scripts/create-test-users.js`, `scripts/seed-test-data.mjs`, `scripts/seed-e2e-data.mjs` referenced in the onboarding skill's quick-start all exist; `npm run type-check` / `npm run lint` / `npm run test:unit` / `npm run test:security` all run successfully as documented in the contribution checklist.
- **JSDoc**: No JSDoc gaps were added to files touched in Waves 1-5 — per project convention, comments were limited to the few places where a non-obvious invariant was introduced (e.g., optimistic-concurrency `.eq("current_step", ...)` filters are self-documenting via their error messages). No further action taken.

### Final Sweep — All Remaining Open Items (across Waves 1-5)

Updated 2026-06-19 after final-sweep execution.

| ID | Severity | Summary | Status |
|---|---|---|---|
| F-046 | Critical | `.env` (live Supabase anon key + placeholder PHI encryption key) tracked in git history | **Deferred** — user decision; requires key rotation by Supabase project owner + git history scrub |
| F-014 | High | Three parallel, non-unified audit-logging systems | **Deferred** — ADR written (see main table); recommended path is a `unified_audit_view` joining all three tiers |
| F-002 | High | 28 `clinical-notes-operations` tests expected unimplemented features | **Fixed** — implemented `auditNoteAccess`, follow-up linkage, bulk export/import, template application, retention policy |
| F-047 | High | 5 overlapping CI workflows | **Fixed** — retired `ci-cd.yml`, `ci.yml`, `test-pyramid.yml` to `workflow_dispatch`; removed push triggers from `automated-testing.yml` |
| F-050 | Medium | `deploy-production.yml` strict type-check non-blocking | **Fixed** — fixed all ~15 strict-mode errors; gate changed to blocking |
| F-038 | Medium | `/health`, `/ready`, `/metrics` not wired to real HTTP routes | **Fixed** — extended `health-check` edge function with `/ready` and `/metrics` path routing |
| F-010 | Medium | `npm audit`: 14 vulns in dev tooling | **Fixed (residual 1 low)** — 13 resolved via `npm audit fix`; 1 low (esbuild 0.27.7, dev-server Windows only) blocked by Vite 7.x `^0.27.0` pin |
| F-042 | Medium | FHIR edge function missing MedicationRequest/Condition/DiagnosticReport | **Fixed** — added all three resource type exports |
| F-043 | Medium | `exportEncounter`: invalid Reference shape, missing `participant.type`, hardcoded class | **Fixed** — derived class from encounter type, added PPRF participant type, fixed Reference shape |
| F-054 | Medium | No distinct staging environment | **Fixed (documented)** — added `environment: staging` to `soak-test.yml`; created `.env.staging.example` with current-posture note |
| F-030 | Low | `CreateInvoiceModal.tsx` subtotal only, no discount/tax preview | **Fixed** — added discount field + live Subtotal/Discount/GST/Total breakdown |
| F-040 | Low | `PatientsPage.tsx` full page reload after registration | **Fixed** — replaced with `queryClient.invalidateQueries` |
| F-020 | Low | `getClientIP()` always returns `'client-ip'` | **Fixed** — returns `null`; `ip_address` typed as `string \| null` |
| F-022 | Low | Route gating client-side only | **Fixed** — spot-check complete; tightened `activity_logs` RLS (migration `20260617000001`) |
| F-008 | Low | `signup-flow.test.tsx` `react-helmet-async` crash | **Fixed** — wrapped test `AllTheProviders` with `<HelmetProvider>` |
| F-044 | Low | FHIR LOINC system not validated on import | **Fixed** — rejects non-LOINC coded observations with HTTP 422 |
| F-045 | Low | FHIR `gender: "unknown"` loses round-trip fidelity | **Fixed** — `mapGenderFromFhir` now preserves `"unknown"` → `"unknown"` |
| F-052 | Low | `automated-testing.yml` all `continue-on-error` | **Fixed (partial)** — security scan gate hardened; perf/accessibility remain informational |

### Overall Initiative Summary (updated 2026-06-19)

- **6 waves + final sweep completed** across 18 of 21 custom skills.
- **54 findings logged** (F-001 through F-054, with F-051 reframed as a false positive).
- **Fixed/Closed**: 52 of 54 items — all Critical/High/Medium findings resolved; all Low items resolved except F-046 (user-deferred) and F-014 (architectural ADR written, implementation deferred).
- **Remaining open**: F-046 (Critical, user-deferred credential rotation) and F-014 (High, architectural consolidation with ADR).
- **Final verification state**: `npm run type-check` ✅ clean, `npm run test:security` 130/130, `npm run test:unit` ~877/886 (F-002 now fixed; ~9 pre-existing unrelated failures remain), `npm audit` 1 low residual (esbuild/Vite 7.x pin).
- **No regressions introduced** at any wave boundary.
