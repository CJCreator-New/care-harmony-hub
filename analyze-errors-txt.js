const fs = require('fs'); const lines = fs.readFileSync('test-run.txt', 'utf8').split('\n'); const assertionErrors = lines.filter(l => l.includes('AssertionError:') || l.includes('Error: ')); const counts = {}; assertionErrors.forEach(e => { const msg = e.trim(); counts[msg] = (counts[msg] || 0) + 1;}); Object.entries(counts).sort((a,b) => b[1] - a[1]).slice(0, 15).forEach(([k,v]) => console.log(v + 'x ' + k.substring(0, 150)));