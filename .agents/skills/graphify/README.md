# Graphify runner

Run the included `run_graphify.py` to generate a minimal graph of the repository.

Outputs are written to `graphify-out/` next to the repository root.

Usage:

```bash
python .agents/skills/graphify/run_graphify.py
```
