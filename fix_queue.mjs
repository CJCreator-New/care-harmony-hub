import fs from 'fs'; let c=fs.readFileSync('src/pages/consultations/ConsultationWorkflowPage.tsx','utf8'); const t='          lab_notified: formData.lab_orders?.length > 0 ? true : formData.lab_notified,\n        });'; const r=t+'\n\n        // Update patient queue\n        if (consultation?.patient_id) {\n          await supabase.from(\\'patient_queue\\').update({ status: \\'completed\\', service_end_time: new Date().toISOString() }).eq(\\'patient_id\\', consultation.patient_id).in(\\'status\\', [\\'in_service\\', \\'called\\', \\'waiting\\']);\n        }'; c=c.replace(t,r); fs.writeFileSync('src/pages/consultations/ConsultationWorkflowPage.tsx',c); console.log('Done');
